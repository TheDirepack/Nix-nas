# Replacement Alternatives for V2 Custom Code

**Principle:** Prefer an existing NixOS/upstream module over a new Python projection. V2 stays a finite compiler over native units — it does not run a daemon.

| Current custom | Lines | Alternative (existing) | How to wire | When to use |
|---|---|---|---|---|
| `nas_v2_accelerator.py` (GPU vendor/CDI selection, 280→80) | 280 | `hardware.nvidia.modesetting`, `hardware.graphics`, `virtualisation.containers.cdi.sources = [pkgs.nvidia-container-toolkit]`, `services.xserver.videoDrivers` | V2 keeps `resources.accelerators[]` as data (`vendor=any/NVIDIA/AMD/Intel`, `quantity`, `required`, `mode=shared/passthrough`). Nix emits `DeviceAllow`/`Environment=NVIDIA_VISIBLE_DEVICES` + `PodmanArgs=--device=nvidia.com/gpu=0`. `is_cdi_selector` stays as regex. | Always — upstream CDI is tested, no need for `_select_vendor` |
| `nas_v2_podman_network.py` (bridge/VLAN/VRF, 514) | 514 | `virtualisation.quadlet.networks.<name>` (`driver="bridge"`, `subnet`, `options.isolate`, `options.vrf`) + `networking.vlans.<vlan>.{id,interface}` + `systemd.network.networks` | V2 keeps `network_policy()`/`requires_firewalld()` validation (30 lines). Nix generates `.network` Quadlet and `systemd-networkd` VLAN. Podman DNS via `virtualisation.podman.defaultNetwork.settings.dns_enabled=true` (NixOS wiki). | Always — `quadlet-nix` (SEIAROTg) is 5k stars, generates same `Network=` refs |
| `nas_v2_firewalld.py` (424) + `nas_v2_firewalld_reconcile.py` | ~600 | `networking.firewall.allowedTCPPorts`/`allowedUDPPorts` + `networking.firewall.extraCommands = ''iptables -A nixos-fw ...''` or `services.firewalld.zones`/`policies` | V2 `compile_projection` becomes `jq -r '.services[] | .listeners[] | .exposure.port'` → `firewall-ports.json` read by Nix at activation (`builtins.fromJSON`). Validate with `firewall-offline-cmd --check-config`. | Always — Nix firewall is declarative, no custom XML |
| `nas_v2_session.py` + `nas_v2_session_projection.py` (829) | 829 | `systemd-run --scope -p DynamicUser=yes -p StateDirectory=nas-v2-sessions/%i -p RemoveOnSuccess=yes` + `virtualisation.oci-containers.containers.<name>.autoStart=false` | `nas_v2_wake.py` (11K→50 lines) just calls `systemd-run` after Caddy `forward_auth`. No V2 session DB, no `nas_v2_session` runner. | Always — `systemd.exec(5)` `DynamicUser`/`RemoveOnSuccess` is upstream |
| `nas_v2_backup.py` + `nas_v2_backup_runtime.py` + `nas_v2_native_dump.py` | ~250 | `services.restic.backups.nas-boot-system.paths = ["/var/lib/postgresql"]`, `services.sanoid.datasets."tank/nas".autosnap=true`, `systemd.services.postgresql.serviceConfig.ExecStartPre = "${pkgs.postgresql}/bin/pg_dump ..."` | Keep only freshness `rm -rf $artifact/* && systemctl restart` + `test -s $artifact/*` (48 lines) as `ExecStartPre`. Delete inventory compiler. | Always — Restic/Sanoid are upstream |
| `nas_v2_caddy.py` (405) | 405 | `services.caddy.virtualHosts.<host>.extraConfig` + `services.caddy.globalConfig` (`nixos/modules/services/web-servers/caddy/default.nix`) + `caddy adapt --adapter caddyfile` | V2 trims to `forward_auth http://127.0.0.1:9000 { uri /outpost.goauthentik.io/auth/caddy; copy_headers X-Authentik-* }`, `stripPrefix`, `requireHeaders`, `onDemandWake`. Move `caddyOnDemandTransport` to `caddy-helpers.nix`. | Always — Authentik docs show identical `forward_auth` template |
| `nas_v2_compose.py` / `nas_v2_quadlet.py` / `nas_v2_libvirt.py` (437/415/340) | ~1200 | `virtualisation.oci-containers`, `virtualisation.quadlet.containers`, `virtualisation.libvirtd` | Make them thin shims (100 lines each) that copy `source` Quadlet/compose XML to `output_dir` and add `PartOf=`; Nix owns `Restart`, `TimeoutStartSec`, `DeviceAllow` | Always — Nix already does `systemd` lowering |
| `nas_v2_systemd.py` (844) | 844 | `systemd.services.<name>.serviceConfig` + `systemd.timers` | Keep only dependency/topology (`network_policy`, `requires_firewalld`, `readiness`) and readiness `ExecStartPre` curl; delegate `DeviceAllow`, `MemoryHigh`, `NoNewPrivileges` to Nix | Always — Nix `systemd` is the spine |

**Not replaced (keep):** `nas_v2_spec.py` (YAML 1.2 + JSON Schema 2020-12 + semantic validation), `nas_v2_bootstrap.py` (seed-once with `flock` + `compile_document`), `nas_v2_apply.py`/`nas_v2_plan.py` (file bundle `os.replace` + `fsync`), `nas_v2_editor.py`/`nas_v2_control.py` (revision CAS).

**Implementation order (bisectable, uses `./tmp` inside repo):**
1. `accelerator` — already done (`d114dc4`, 80 lines).
2. `podman/firewall` — keep `network_policy`/`requires_firewalld` validation, delete 600 lines of VLAN VRF generation (next commit).
3. `sessions` — delete 800 lines, keep 50-line `wake` → `systemd-run`.
4. `backup` — delete inventory, keep 48-line freshness `ExecStartPre`.
5. `caddy` — trim to 150 lines.

**Validation for each:** `nix develop .#test -c ./scripts/run-unit-tests.py --jobs 4` + `node --test tests/js/*.test.mjs` must stay green; `./tmp` (not `/tmp`) for `package-release` artifacts; `nix develop .#qemu-test -c ./scripts/qemu-test.sh all` remains non-blocking (currently broken).
