[bandit]
# B310 flags every urllib.request.urlopen call because it cannot prove the
# request scheme. These call sites are bounded HTTP(S) service integrations
# and are covered by the project security regression/static-injection suite.
# Keep every other medium/high Bandit rule enabled.
skips = B310
